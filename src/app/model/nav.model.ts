export interface NavItem {
  ariaCurrent: string;
  routerLinkActive: string[];
  routerLinkActiveOptions: { exact: boolean };
  routerLink: string;
  buttonClass: string;
  buttonText: string;
  icon: string;
}
